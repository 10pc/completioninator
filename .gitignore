__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
build/
dist/
data/*
fixtures/
.env
# Local-only compose overrides (per-environment tweaks like group_add).
# Never commit these; each host keeps its own.
compose.override.yml
